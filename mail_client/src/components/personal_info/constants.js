// Max image size for uploads (5MB)
export const MAX_IMAGE_SIZE_BYTES = 5 * 1024 * 1024;

// Supported image types for profile pictures
export const ALLOWED_IMAGE_TYPES = [
  'image/jpeg',
  'image/png',
  'image/webp'
];

// Domain used for generating user emails
export const EMAIL_DOMAIN = 'mailme.com';
